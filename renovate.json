{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended"
  ],
  "pip_requirements": {
    "fileMatch": ["(^|/)requirements\\.txt$"]
  },
  "packageRules": [
    {
      "description": "Keep requirements.lock in step with requirements.txt (issue #101). The lock is generated by pip-compile and must be regenerated whenever a direct dependency changes; a Renovate bump of requirements.txt without the lock fails the CI gate's hash check.",
      "matchManagers": ["pip_requirements", "pip-compile"],
      "matchFileNames": ["requirements.txt", "requirements.lock"],
      "groupName": "python dependencies (lock in step)"
    }
  ]
}
